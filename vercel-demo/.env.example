# اگر خالی بماند، دمو با داده‌ی نمایشی (mock) کار می‌کند.
# در صورت پر شدن، به endpointهای عمومی محدود ORDS وصل می‌شود
# (فقط GET availability و POST requests — هرگز CRUD رئیس یا credential دیتابیس).
ORACLE_API_BASE_URL=
